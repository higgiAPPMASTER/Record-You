kind = "web"
previewPath = "/"
title = "Song Recorder"
version = "1.0.0"
id = "artifacts/music-recorder"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22278

[services.development]
run = "pnpm --filter @workspace/music-recorder run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/music-recorder", "run", "build" ]
publicDir = "artifacts/music-recorder/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22278"
BASE_PATH = "/"
